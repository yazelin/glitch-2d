# 下一步

## 正面掌心的手（最優先，0.5.0 沒做到）

0.5.0 上線的桌寵**揮的是手背**。把垂手的 `sleeve-left-v8.png` 彎上去，舉起來的仍然是側面的手：手背朝外、五指斜著。`pet-plain.webp` 那隻是**掌心正對鏡頭**，兩者是不同的觀看角度，旋轉變不出來，這是 Live2D 也用差分圖解決的那一類。

使用者一眼會看的就是這件事，而 `check-wave.mjs` 的六條檢查沒有一條在量它。

決定的做法：

1. 依據取自 `pet-plain.webp` 濾出來的掌心（(0,291)–(132,429)），那是正典形狀。照它重畫一張，對齊新畫風的部分是袖口與線條粗細，掌心本身照正典。
2. 做成可切換部件 `hand-open`，`node: "arm-left"`，排在 `arm-left` 之後、`torso` 之前。自己一張貼圖，袖子一個像素都不動。
3. 走 `partOpacity` 依參數切透明度，跟 `closed-eye` 吃 `eyeOpen` 同一套。`armFold` 過門檻才淡入，垂著與低角度維持側手掌。
4. 驗收：舉手最高點的定格圖跟 `pet-plain.webp` 並排，掌心的大小、角度、手腕接袖口的位置都要對得上；另外跑 `check-wave.mjs` 確認新部件沒有被取景切到。

**提示詞不可以沿用手背那組錨點。**「五指、無色指甲」是手背才看得到的規格，掌心沒有指甲，照抄會產出手背或畫出不該存在的指甲。掌心該寫的是掌紋、指腹、拇指與其餘四指的關係，以及袖口從手腕哪一側露出來。

直接把濾出來的圖當成品不行，實測過：只濾膚色會把粉色線稿邊一起濾掉變成毛邊；留住線稿之後放大 1.62 倍，線條明顯比周圍的新素材糊，而且會連著舊的斜背帶碎片。對照圖與切好的素材在 `test-results/palm/`。

這是改角色素材，動手前要先給 yazelin 過目。

## 帽T 的腋下沒有為抬手畫過

上臂往外抬超過 0.12 到 0.17 之間那道線，帽T 貼圖上被 `clearPolygons` 挖掉的袖根 socket 就會露出來。那塊洞不挖也不行，垂手時兩肩會各露一塊深色橢圓。所以現在的抬幅 0.12 是素材給的天花板，不是設計選的。

要抬更開就得替帽T 補畫腋下：在 `torso.png` 的 socket 區域（圖集 x 266..360 與 895..985、y 276..526）畫上抬手時會露出來的那一段袖根與腋下衣褶，然後把 `clearPolygons` 收窄到只蓋住垂手時會穿幫的上緣。補完之後 `check-wave.mjs` 的袖根破口檢查會自己放行更大的角度。

這也是改角色素材，要先過目。

## 展到另一隻手與兩條腿

`bend` 目前只掛在 `arm-left`。`arm-right` 的 `joints` 已經有了，補上 `bend` 就能動。兩條腿沒有 `joints`，要先標。展開前先確認 `check-wave.mjs` 那套量法對右手也成立，它現在的取景切邊檢查是整張畫面，可以直接沿用。

## 表情差分的兩張圖

切換機制在 0.5.0 做好了（`slot` 與 `variant`），缺的是圖：

- happy 的笑眼（向上彎的弧線）
- error 的螺旋眼

兩張都畫進 `face-features.png` 現有的空白區，用同一個綠底（chroma `[17,239,15]`），像素密度對齊現有的眼睛（282×208 的圖對到 121×90 的角色框）。`rect` 與 `adjustment` 從 `eye-left` / `eye-right` 整組複製即可，前提是新圖在自己的 uv 框裡的佔比跟舊圖一樣。`type` 不可以寫 `eye`，那會被 `eyeOpen` 再壓一次；比照 `closed-eye` 另立一個不吃 aperture 的型別。

這也是改角色素材，要 yazelin 先看過。

## 腿的束口（未拍板，2026-09-11 暫停）

0.4.4 的 `legs-v5.png` 已經把大腿加粗、吊襪帶壓出勒痕、束口稍微收窄。使用者看的是 0.4.2 的舊腿，問「能不能只把小腿的束口束起來、其他不動」。當下決定腿先不動，等他看過 0.4.4 的實際結果再說。要動的話兩條路：

1. 從 `legs-v4.png`（0.4.2）出發，只把羅紋束口收窄，大腿與勒痕都不加。
2. 留 `legs-v5.png`，只把羅紋再收窄一些。

素材要用 codex 重畫，不要用 rig 參數去拉寬拉窄——0.4.3 那樣做把小腿拉壞過。
